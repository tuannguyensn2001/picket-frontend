import type { Meta } from '@storybook/react';
import { HeaderCenterItem } from './header-center-item';

const Story: Meta<typeof HeaderCenterItem> = {
  component: HeaderCenterItem,
  title: 'HeaderCenterItem',
};
export default Story;

export const Primary = {
  args: {},
};
